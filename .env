# Flask/Socket.IO app secrets
SECRET_KEY=change-me-to-a-long-random-string

# CORS origins (comma-separated); note: app currently not reading this yet
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# Xray binary path (auto-download will attempt to place it here if missing)
XRAY_PATH=./xray

# Optional logging level for future use
LOG_LEVEL=INFO